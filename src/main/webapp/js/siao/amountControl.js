{/* <script>
							const 
							plusElement = document.getElementById("plus"),
							numElement = document.getElementById("num"),
							minusElement = document.getElementById("minus");
							console.log(numElement.value)
							let buyingNumber = num.innerHTML;
							let a = [];
							for (i = 0; i <= numElement.length; i++) {
								a[i] = numElement[i].value;
								plusElement[i].addEventListener('click', () => {
									a[i]++
									a[i] = (a[i] < 10) ?  a[i]
										: a[i];
										numElement[i].innerHTML = a[i];
									console.log(a[i]);
								})
								minusElement[i].addEventListener('click', () => {
									a[i]--
									if (a[i] > 0 && a[i] < 10) {
										numElement[i].innerHTML =  a[i];
									}
									else if (a < 1) {
										a[i] = 1;
										numElement[i].innerHTML = a[i];
									}
									else {
										numElement[i].innerHTML = a[i];
									}
								})
							}
						</script> */}